services:
  sf6-tracker:
    image: ghcr.io/lp0101/sf6-mr-tracker:latest
    container_name: sf6-mr-tracker
    build: .
    env_file: .env
    volumes:
      - ./credentials.json:/app/credentials.json:ro
    environment:
      - TZ=America/Toronto
    restart: unless-stopped
